import { MyListGeneriqueObject } from './my-list-generique-object';

describe('MyListGeneriqueObject', () => {
  it('should create an instance', () => {
    expect(new MyListGeneriqueObject()).toBeTruthy();
  });
});
